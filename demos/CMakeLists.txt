find_package(GLUT)
if (GLUT_FOUND)
    add_subdirectory(main_demo)
else()
    message("Demos which use GLUT will be not build")
endif()
add_subdirectory(light_demo)
add_subdirectory(triangle)
add_subdirectory(shader_introspection_demo)
add_subdirectory(text)
add_subdirectory(qt)
